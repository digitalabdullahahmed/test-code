# App Router 2026

- Use `app/`, not `pages/`.
- Prefer Server Components by default.
- Keep `app/` thin and push feature logic into `modules/`.
- Use route groups, layouts, loading, error, and not-found boundaries deliberately.
- Make caching behavior explicit.
