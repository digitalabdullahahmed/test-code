# Next.js SEO Files

- Use `generateMetadata` deliberately.
- Use `app/robots.ts` or `robots.txt`.
- Use `app/sitemap.ts` or nested sitemap files.
- Keep canonical strategy explicit.
- Decide indexing intent for each surface.
