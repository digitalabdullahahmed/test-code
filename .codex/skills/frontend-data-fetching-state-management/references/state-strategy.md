# State Strategy

- Server Components first.
- Use client fetching only when needed.
- Use TanStack Query for complex client-side server state.
- Use Zustand sparingly for real shared client state.
- Keep cache and revalidation explicit.
