# Acceptance Criteria Rules

- define what must be true for the task to count as complete before implementation finishes
- include user-visible behavior, data behavior, auth behavior, error behavior, and verification expectations where relevant
- keep acceptance criteria concrete enough to verify
- if the request is broad, infer a minimal professional acceptance set instead of leaving completion fuzzy
- do not claim completion when key acceptance criteria were never implemented or checked
